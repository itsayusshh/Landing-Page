function Button({ buttonType = "button" }) {
  return (
    <>
      <button type={buttonType} className="NB-2container-buttonss">
        Submit
      </button>
    </>
  );
}

export default Button;
